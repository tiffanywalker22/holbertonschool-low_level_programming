#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include "main.h"
/* more headers goes there */

/**
 * _islower - function checks for lowercase character
 *
 * Return: Always 0 (Success)
 */
	int _islower(int c)
	{
		return (c >= 'a' && c <= 'z');
	}
{
		putchar('\n');
}
